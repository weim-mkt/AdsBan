# Project Memory — AdsBan

Corrections and learned facts that persist across sessions.
When a mistake is corrected, append a `[LEARN:category]` entry below.

Cross-session pointer: harness memory at `/home/weimiao/.claude/projects/-media-psf-data-AdsBan/memory/` holds user/project/reference memory (not committed). This file is for project-specific learnings that should travel with the repo.

---

<!-- Append new entries below. Most recent at bottom. Keep file under 200 lines. -->

## Workflow

[LEARN:workflow] 2026-04-22 — adopted Claude Code academic workflow (pedrohcgs/claude-code-my-workflow v1.7.0 + 14 commits, HEAD 3e4b113). Template copied into project root; `claude-code-my-workflow/` symlink kept for upstream diffing. Plan-first + contractor mode is the default. `set.seed(888)` per randomness step; paths via `here::here()`; figures `ggthemes::theme_stata()` + `bg = "transparent"`.
